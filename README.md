# spinout-fair-ride
Automated planner that generates daily sailing recommendations for Spinout Fair Ride. A Python script runs via GitHub Actions to produce a public JSON file with route suggestions, timings, and difficulty ratings.
 
